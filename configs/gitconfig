[user]
    name = Dylan Griffith
    email = dgriffith@groupon.com
[core]
    editor = /usr/local/bin/vim
	excludesfile = ~/.gitignore_global
[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
    ui = auto
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    df = diff
    dc = diff --cached
    fall = fetch --all
    cim = commit -m
[push]
	default = simple
[credential]
	helper = osxkeychain
